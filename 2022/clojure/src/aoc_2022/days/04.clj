(ns aoc-2022.days.04 
  (:require [aoc-2022.util :as util]
            [clojure.string :as str]
            [clojure.set]))

(defn p1 
  ([] (p1 "input"))
  ([file] ))

(defn p2 
  ([] (p2 "input"))
  ([file] ))